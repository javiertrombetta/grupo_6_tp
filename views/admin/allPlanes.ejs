<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todos los Usuarios</title>
    <link rel="stylesheet" href="/css/normalize.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/header-javier.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
    <link rel="icon" href="/images/favicon/favicon.ico" type="image/x-icon" />
</head>
<body>
    <header>
        <%- include('../partials/header.ejs') %>
    </header>

    <% if (locals.isAdmin && planes) { %>
        <% for( let i = 0; i < planes.length; i++ ) { %>
            <div>
                <p><%= planes[i].id  %> </p>
                <p><%= planes[i].nombre_plan %> </p>
                <p><%= planes[i].velocidad_descarga %> </p>
                <p><%= planes[i].velocidad_subida %> </p>
                <p><%= planes[i].atencion_cliente %> </p>
                <p><%= planes[i].wifi %> </p>
            </div>
        <% } %>

    <% }else{ %>
        <p>No tienes permiso para acceder a esta pagina</p>

        <% } %>
    <footer>
        <%- include('../partials/footer.ejs') %>
    </footer>
</body>
</html>